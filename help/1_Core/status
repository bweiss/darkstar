Synopsis:
   status [-q] [<status>]

Description:
   Attempts to change the current status line to <status>.

   If the -q option is specified the status change will be silent.

   If no <status> is specified the user will be prompted to choose one from
   the list of available statbars. Either name or number will be accepted.

See Also:
   ds:theme(1);

