Synopsis:
   loadmod [<module>]

Description:
   This command is used to load a new module into Darkstar, thus adding new
   features.

   If <module> is not specified the current module list will be displayed
   and the user will be asked to choose one or more modules to load, either
   by name or number.

See Also:
   ds:modlist(1);  ds:reloadmod(1);  ds:unloadmod(1);
   ds:dset(1) auto_load_modules

