Synopsis:
   loadedmodules
   loadedmods

Description:
   Displays the list of currently loaded modules.

See Also:
   ds:loadmod(1);  ds:reloadmod(1);  ds:unloadmod(1);
   ds:dset(1) auto_load_modules

