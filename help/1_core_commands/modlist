Synopsis:
   modlist

Description:
   Displays the current module list. This shows both loaded and non-loaded
   modules as well as whether or not they are currently on the list of
   auto-load modules.

See Also:
   ds:loadmod(1);  ds:reloadmod(1);  ds:unloadmod(1);
   ds:dset(1) auto_load_modules

