Synopsis:
   fset set [<value>]

Description:
   Controls the appearance of /DSET and /FSET output when the variable in
   question has a value.

Parameters:
   $1    Variable
   $2-   Value

See Also:
   ds:dset(1);  ds:fset(1);  ds:fset(1) set_novalue

