Synopsis:
   dset auto_load_modules [<module> [<module> ...]]

Description:
   This determines which modules will be automatically loaded on startup.
   This only applies if LOAD_PROMPT is OFF or if the 'A' option is
   specified at the load prompt.

See Also:
   ds:dset(1) load_prompt

