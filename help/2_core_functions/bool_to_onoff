Synopsis:
   $bool_to_onoff(1|0)

Description:
   Converts 1/0 to ON/OFF.

Returns:
   Returns "ON" if passed "1", "OFF" if passed "0", or its arguments
   unchanged if they are neither "0" nor "1".

See Also:
   ds:bool_to_num(2)

